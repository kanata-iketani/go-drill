{
  "lesson": 7,
  "chapter": 5,
  "title": "append で結合（s2...）",
  "priority": "🟡",
  "text": "スライス同士の結合も `append` で行います。第 2 引数にスライス名と `...` を付けると、要素を展開して 1 つずつ追加した扱いになります。Python の `a + b` や `a.extend(b)` に当たる書き方です。`...` を忘れると「型が違う」というコンパイルエラーになります。🟡 なぜ `...` が必要なのか（append はスライスではなく要素を受け取る関数だから）を理屈で理解しましょう。\n\nPython ではこう書いた:\n\n```python\nc = a + b\n```\n\nGo ではこう書く:\n\n```go\nc := append(a, b...)\n```",
  "sample": "// lesson07 ch05: append で結合（s2...）\npackage main\n\nimport \"fmt\"\n\nfunc main() {\n\ta := []int{1, 2}\n\tb := []int{3, 4}\n\n\t// b... で b の要素を展開し、1 つずつ追加した扱いになります\n\tc := append(a, b...)\n\tfmt.Println(c)\n\tfmt.Println(len(c))\n\n\t// 文字列のスライスでも同じです\n\tx := []string{\"朝\", \"昼\"}\n\tx = append(x, []string{\"夜\"}...)\n\tfmt.Println(x)\n}\n",
  "exercise": {
    "description": "スライス `a := []int{1, 2, 3}` と `b := []int{4, 5}` を作り、`append` と `...` で結合したスライスを出力してください。\n\n**期待出力**\n\n```text\n[1 2 3 4 5]\n```",
    "starter": "package main\n\nimport \"fmt\"\n\nfunc main() {\n\t// ここに書く\n\t_ = fmt.Println\n}\n",
    "stdin": "",
    "expected_stdout": "[1 2 3 4 5]\n",
    "answer": "// lesson07 ch05 解答\npackage main\n\nimport \"fmt\"\n\nfunc main() {\n\ta := []int{1, 2, 3}\n\tb := []int{4, 5}\n\n\t// Python の a + b に当たる結合。append は要素を受け取る関数なので、\n\t// b... と展開して「b の要素を 1 つずつ追加する」形にします。\n\tc := append(a, b...)\n\tfmt.Println(c)\n}\n"
  }
}
