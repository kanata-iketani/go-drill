# lesson07 ch06 演習: 要素の削除（pop・remove・del はない）

スライス `[]int{10, 20, 30, 40}` を作り、`slices.Delete` で 1 番目の要素（20）を削除してスライス全体を出力し、続けて末尾削除 `s[:len(s)-1]` を行ってもう一度スライス全体を出力してください。`slices` を import に追加してください。

**期待出力**

```text
[10 30 40]
[10 30]
```
