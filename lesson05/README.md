# Lesson 05 文字列

Go の文字列で最重要の注意点は、`s[i]` が文字ではなく**バイト**を返すことです。日本語を文字単位で扱うには `[]rune` に変換します。f文字列に相当するのは `fmt.Sprintf` です。

## ch01 文字列リテラルと + 結合

Go の文字列は `"..."`（ダブルクォート）で書きます。Python のようにシングルクォートで文字列は作れません（Go の `'a'` は 1 文字の rune という別物になります）。`+` で連結できるのは Python と同じです。改行を含めたいときは `\n`、そのまま複数行書きたいときはバッククォート `` ` `` を使います。🔴 「文字列は必ずダブルクォート」を手に覚えさせましょう。

Python ではこう書いた:

```python
s = 'Hello' + "World"
```

Go ではこう書く:

```go
s := "Hello" + "World" // ダブルクォートのみ
```

## ch02 len と s[i]（バイト）

`len(s)` は文字数ではなく**バイト数**（データの大きさの単位。ASCII 文字は 1 文字 1 バイト、日本語は UTF-8 で 1 文字 3 バイト）を返します。同様に `s[i]` も i 番目の文字ではなく i 番目のバイトを返します。英数字だけなら Python と同じ感覚で動きますが、日本語が混ざると期待とずれます。🟡 「文字列の実体はバイト列」という理屈を理解しましょう。

Python ではこう書いた:

```python
s = "こんにちは"
print(len(s))  # 5
```

Go ではこう書く:

```go
s := "こんにちは"
fmt.Println(len(s)) // 15（バイト数）
```

## ch03 []rune と日本語

日本語を文字単位で扱うには `[]rune(s)` で **rune**（1 文字を表す Go の型。Unicode のコードポイント 1 つ分）のスライスに変換します。変換後は `len` が文字数になり、`[i]` で i 番目の文字を取れます。取り出した rune を文字列として表示するには `string(r[0])` と戻します。「日本語が来たら []rune」は Go の定石です。🟡 なぜ変換が必要か（ch02 のバイトの話）とセットで理解しましょう。

Python ではこう書いた:

```python
s = "こんにちは"
print(s[0])  # こ
```

Go ではこう書く:

```go
r := []rune("こんにちは")
fmt.Println(len(r))       // 5（文字数）
fmt.Println(string(r[0])) // こ
```

## ch04 strings.Repeat

Python の `"ab" * 3` のような文字列の掛け算は Go にはありません。代わりに `strings` パッケージの `strings.Repeat(s, n)` を使います。`strings` は文字列操作の関数を集めた標準ライブラリで、このあとのレッスンでも繰り返し登場します。⚪ 「* の代わりに strings.Repeat」と存在だけ知っておけば十分です。

Python ではこう書いた:

```python
print("=" * 10)
```

Go ではこう書く:

```go
import "strings"

fmt.Println(strings.Repeat("=", 10))
```

## ch05 fmt.Sprintf（f文字列の代わり）

Python の f文字列 `f"{name}さん"` に相当するのが `fmt.Sprintf` です。`Printf` と同じ書式（`%s` `%d` `%.1f` など）で文字列を**組み立てて返す**関数で、画面には出力しません。「表示するなら Printf、変数に入れるなら Sprintf」と使い分けます。🔴 実務でもスキルチェックでも頻出なので、手が覚えるまで書きましょう。

Python ではこう書いた:

```python
msg = f"{name}さんは{age}歳です"
```

Go ではこう書く:

```go
msg := fmt.Sprintf("%sさんは%d歳です", name, age)
```
