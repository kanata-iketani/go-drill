# lesson09 ch03 演習: 配列 [3]int（固定長・値型）

配列 `a := [3]int{1, 2, 3}` を作り、`b := a` でコピーしてから `b` の 0 番目を `99` に書き換え、1 行目に `a` を、2 行目に `b` を出力して、コピー元が変わっていないことを確認してください。

**期待出力**

```text
[1 2 3]
[99 2 3]
```
