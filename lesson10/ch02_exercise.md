# lesson10 ch02 演習: else if と else（elif の代わり）

変数 `n` に `7` を入れて、`n` が 10 以上なら `big`、そうでなく 5 以上なら `medium`、それ以外なら `small` と出力するプログラムを `if` / `else if` / `else` で書いてください。

**期待出力**

```text
medium
```
