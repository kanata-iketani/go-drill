# lesson10 ch03 演習: 条件の前の1文（if v := f(); v > 0）

変数 `word` に `"golang"` を入れて、条件の前の 1 文で `l := len(word)` を書き、`l` が 5 以上なら `fmt.Println("5文字以上:", l)` の形で出力、そうでなければ `5文字未満` と出力してください。

**期待出力**

```text
5文字以上: 6
```
