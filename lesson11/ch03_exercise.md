# lesson11 ch03 演習: slices.Contains（スライスの in の代わり）

スライス `nums := []int{2, 4, 6, 8}` を作り、`slices.Contains` を使って次の 2 つの結果を順に出力してください。

1. `nums` に `6` が含まれるか
2. `nums` に `5` が含まれるか

**期待出力**

```text
true
false
```
