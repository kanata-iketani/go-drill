# lesson11 ch04 演習: strings.Contains（文字列の in の代わり）

変数 `s` に `"golang is fun"` を入れて、`strings.Contains` を使って次の 2 つの結果を順に出力してください。

1. `s` に `"go"` が含まれるか
2. `s` に `"python"` が含まれるか

**期待出力**

```text
true
false
```
