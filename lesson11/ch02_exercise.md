# lesson11 ch02 演習: && と || と !（and or not の代わり）

変数 `x` に `7`、変数 `y` に `12` を入れて、次の 3 つの式の結果を順に出力してください。

1. `x > 5 && y > 10`
2. `x > 10 || y > 10`
3. `!(x == 7)`

**期待出力**

```text
true
true
false
```
