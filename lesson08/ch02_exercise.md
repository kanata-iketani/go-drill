# lesson08 ch02 演習: strings.Split（文字列→スライス）

文字列 `"apple:banana:cherry"` を `strings.Split` で `:` 区切りに分割し、1 行目にスライス全体を、2 行目に 0 番目の要素を出力してください。`strings` を import に追加してください。

**期待出力**

```text
[apple banana cherry]
apple
```
