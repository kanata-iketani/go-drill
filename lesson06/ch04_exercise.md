# lesson06 ch04 演習: strconv.FormatFloat（float64→文字列）

変数 `height` に `169.5` を入れ、`strconv.FormatFloat(height, 'f', 1, 64)` で文字列に変換してから、文字列連結 `+` で `身長は169.5cmです` と出力してください。`strconv` を import に追加してください。

**期待出力**

```text
身長は169.5cmです
```
