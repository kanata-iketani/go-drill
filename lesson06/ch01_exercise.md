# lesson06 ch01 演習: strconv.Itoa（数値→文字列）

変数 `count` に `15` を入れ、`strconv.Itoa` で文字列に変換してから、文字列連結 `+` を使って `残り15個です` と出力してください（文をそのまま `Println` に書くのではなく、必ず `Itoa` を使うこと）。`strconv` を import に追加してください。

**期待出力**

```text
残り15個です
```
