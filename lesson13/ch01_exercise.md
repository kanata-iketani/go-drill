# lesson13 ch01 演習: for i, v := range スライス（enumerate 相当）

スライス `colors := []string{"red", "green", "blue"}` を作り、`for i, v := range` を使って `fmt.Println(i, v)` の形で「インデックス 値」を 1 行ずつ出力してください。

**期待出力**

```text
0 red
1 green
2 blue
```
